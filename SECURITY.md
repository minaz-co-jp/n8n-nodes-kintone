# Security Policy

Do not report API tokens, credentials, customer data, or production payloads in public Issues or Pull Requests.

For normal bugs, open an Issue using sanitized reproduction data.

For a security vulnerability that could expose credentials or customer data, contact the repository maintainers privately before publishing details.

The node must never include API tokens or credential headers in user-visible errors or logs.
